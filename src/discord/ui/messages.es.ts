/**
 * The ONLY file allowed to hold user-facing Discord copy (spec:
 * User-Facing Copy Language, rule 5; design-part4 §H is the full copy
 * register that this file grows into as each phase lands its own
 * reasons). Phase 1 seeds it with a single placeholder line for command
 * bodies that are still stubs — `MusicService`, the queue, and every typed
 * rejection/failure reason land Phase 3+.
 */
export const NOT_YET_IMPLEMENTED =
  "🚧 Este comando todavía no está disponible — se habilita en una próxima versión.";
